#include <bits/stdc++.h>
using namespace std;
int n,i;
int main(){
    cin>>n;
    vector<int> vec(n);
    for(i=0;i<n;i++)
    {
        cin>>vec[i];
    }
    sort(vec.begin(),vec.end());
    for(i=0;i<n;i++)
    {
        cout<<vec[i]<<" ";
    }
    return 0;
}
